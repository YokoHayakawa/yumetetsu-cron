
/**
 * Fix datetime of provided fields to match kintone format.
 * Kintone format: yyyy-MM-dd HH:mm
 * @param path
 * @param fields
 */
const fixCSVDateTime = (path: string, fields: string[]) => {
  console.log(path);
};
